{
  "test_folder": "TestTagging",
  "tag_list": "Invoice, Receipt, Contract, Resume, Cover Letter, Report, Memo, Presentation, Spreadsheet, Code, Configuration, Log, Manual, Article, Note, Email, Schedule, Budget, Proposal, Agreement, Statement, Form, ID, Certificate, Ticket, Map, Diagram, Photo, Screenshot, Personal, Work, Finance, Legal, Medical, Education, Travel, Shopping, Project, Meeting, Research, Marketing, Sales, HR, Admin, Security, Urgent, Draft, Final, Review, Template, Archived",
  "models": [
    {
      "id": "tinyllama",
      "name": "tinyllama",
      "file_types": ["text"]
    },
    {
      "id": "phi4",
      "name": "phi4",
      "file_types": ["text"]
    },
    {
      "id": "gemma2_9b",
      "name": "gemma2:9b",
      "file_types": ["text"]
    }
  ],
  "prompts": [
    {
      "id": "constrained_default",
      "prompt": "Select 3-7 relevant tags for this text ONLY from the following list: {tag_list}. Return them as a comma-separated list.\n\nContent: {text}\n\nTags:",
      "options": {
        "temperature": 0.3,
        "num_predict": 50
      }
    },
    {
      "id": "constrained_strict",
      "prompt": "You are a strict classification system. You must ONLY use tags from this allowed list: {tag_list}. Analyze the content and output the matching tags as a comma-separated list.\n\nContent: {text}\n\nTags:",
      "options": {
        "temperature": 0.1,
        "num_predict": 50
      }
    },
    {
      "id": "constrained_type_topic",
      "prompt": "Analyze this document. First select a Document Type tag from the list, then select Topic tags. Use ONLY tags from this list: {tag_list}.\n\nContent: {text}\n\nTags:",
      "options": {
        "temperature": 0.2,
        "num_predict": 60
      }
    },
    {
      "id": "constrained_best_fit",
      "prompt": "Which of these tags best describe the content? List: {tag_list}. Pick the top 5 most accurate tags.\n\nContent: {text}\n\nTags:",
      "options": {
        "temperature": 0.25,
        "num_predict": 50
      }
    },
    {
      "id": "constrained_creative",
      "prompt": "From the following list, choose the tags that capture the essence of this document. You may be slightly flexible if the exact match isn't there, but prefer these tags: {tag_list}.\n\nContent: {text}\n\nTags:",
      "options": {
        "temperature": 0.5,
        "num_predict": 60
      }
    }
  ]
}
